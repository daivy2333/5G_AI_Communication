<!-- generated-by: gsd-doc-writer -->
# 性能参数文档

**版本**: V2.1
**更新日期**: 2026-06-30
**数据来源**: `docs/experiments/real/summary.json` 与 `docs/experiments/real/data/*.csv`，真实 PyTorch 训练（CUDA, 50 epochs, 8000 信道估计训练样本）

---

## 目录

1. [信道估计性能](#1-信道估计性能)
2. [信号检测性能](#2-信号检测性能)
3. [调制识别性能](#3-调制识别性能)
4. [资源调度性能](#4-资源调度性能)
5. [系统整体性能总结](#5-系统整体性能总结)
6. [测试环境信息](#6-测试环境信息)

---

## 1. 信道估计性能

### 1.1 整体 NMSE 对比（真实训练结果）

训练条件：Transformer (4层, 8头, d=128), 8000 训练样本, 50 epochs, 5G UMa 信道, SNR ∈ [−10, 30] dB。结果来自 `docs/experiments/real/summary.json` 的 `channel_estimation.evaluation` 字段。

| 方法 | 整体 NMSE (dB) | 说明 |
|------|---------------|------|
| LMMSE | -0.70 | 线性最小均方误差估计 |
| MMSE | -0.26 | 最小均方误差估计 |
| LS | +0.24 | 最小二乘估计 |
| **AI-ChannelNet** | **-20.42** | Transformer AI 估计 |

> AI-ChannelNet 比 LMMSE 提升 **19.7 dB**，超过设计目标（≥7 dB 提升）。

### 1.2 各 SNR 区间 NMSE（真实训练结果）

分段结果来自 `docs/experiments/real/data/channel_estimation_by_snr.csv`。

| SNR 区间 (dB) | LS (dB) | MMSE (dB) | LMMSE (dB) | AI-ChannelNet (dB) |
|--------------|---------|-----------|------------|-------------------|
| [-10, 0] | +5.62 | +5.11 | +4.64 | **-15.38** |
| [0, 10] | -4.04 | -4.51 | -4.87 | **-24.63** |
| [10, 20] | -14.57 | -14.65 | -14.03 | **-28.35** |
| [20, 30] | -23.35 | -21.30 | -18.11 | **-28.92** |

### 1.3 信道估计配置参数

| 参数 | 数值 | 说明 |
|------|------|------|
| 嵌入维度 | 128 | Transformer d_model |
| 注意力头数 | 8 | 多头自注意力 |
| Transformer 层数 | 4 | 编码器深度 |
| FFN 隐藏维 | 512 | d_model × 4 |
| Dropout 率 | 0.1 | 正则化 |
| 批量大小 | 64 | 训练批量 |
| 学习率 | 1e-4 | Adam |
| 导频间隔 | 8 | 子载波间隔 |
| 信道抽头 | 16 | 多径数量 |
| 输入预处理 | received × conj(pilot) | 导频共轭消去调制

---

## 2. 信号检测性能

信号检测与调制识别是两个独立任务，分别由 `detector.py`（CNN-LSTM，二分类 BPSK 比特检测）和 `recognizer.py`（CNN，5 类调制识别）实现。

### 2.1 BPSK 比特检测（真实训练结果）

训练条件：CNN-LSTM (64→128→256 filters, Bi-LSTM 128), 50 epochs, 5000 训练样本。评估结果来自 `docs/experiments/real/summary.json` 与 `docs/experiments/real/data/signal_detection_results.csv`。

| 指标 | 数值 | 说明 |
|------|------|------|
| 准确率 | **98.08%** | (TP + TN) / Total |
| 精确率 | 100.00% | TP / (TP + FP) |
| 召回率 | 96.16% | TP / (TP + FN) |
| F1 分数 | **98.04%** | 2·P·R / (P + R) |

### 2.2 5 类调制识别（真实训练结果）

训练条件：CNN (k=7/5/3, 64→128→256 filters), 50 epochs, 基础 1000/类训练样本，并使用 256QAM×2 与 64QAM×1.5 过采样。评估结果来自 `docs/experiments/real/data/modulation_recognition_results.csv`。

| 调制方式 | 准确率 |
|----------|--------|
| **BPSK** | **98.93%** |
| **QPSK** | **82.73%** |
| **16QAM** | **67.53%** |
| **64QAM** | **52.53%** |
| **256QAM** | **82.80%** |
| **总体** | **76.91%** |

> 优化方案: FC 容量 512 (原 256) + 256QAM×2 过采样 + 64QAM×1.5 过采样。此方案经 Focal Loss / 分组多出口架构对比验证，为当前最优。

### 2.3 信号检测网络配置

| 参数 | 数值 | 说明 |
|------|------|------|
| CNN滤波器 | 64 → 128 → 256 | 三级卷积特征提取 |
| 卷积核大小 | 3 × 1 | 局部特征提取 |
| LSTM层数 | 2 | 双向时序建模 |
| LSTM单元数 | 128 | 隐藏状态维度 |
| Dropout率 | 0.3 | 防止过拟合 |
| 批量大小 | 128 | 训练批量 |
| 学习率 | 1e-3 | Adam优化器 |

---

## 3. 资源调度性能

### 3.1 PPO 算法训练结果（真实训练）

结果来自 `docs/experiments/real/summary.json` 的 `resource_scheduling` 字段与 `docs/experiments/real/data/resource_scheduling_reward.csv`。

| 指标 | 数值 | 说明 |
|------|------|------|
| 算法 | PPO | 近端策略优化 (stable-baselines3) |
| 平均奖励 (last 20) | **2.393** | 训练收敛后 |
| 最终公平性 | **0.8717** | Jain Fairness Index |
| 折扣因子 (γ) | 0.99 | 长期收益权重 |
| GAE 参数 (λ) | 0.95 | 优势函数估计 |
| 裁剪范围 (ε) | 0.2 | 策略更新限制 |
| 训练步数 | 10,000 | 20 用户 × 100 RB

### 3.2 资源调度环境参数

| 参数 | 数值 | 说明 |
|------|------|------|
| 用户数量 | 20 | 仿真用户数 |
| 资源块数量 | 100 | RB 总数 |
| 系统带宽 | 100 MHz | 与 §1.3 一致 |
| 最大发射功率 | 33 dBm | 功率上限 |

### 3.3 奖励函数权重

| 奖励分量 | 权重 | 说明 |
|----------|------|------|
| 吞吐量 | 1.0 | 主要优化目标 |
| 公平性 (Jain) | 0.3 | 用户公平性保障 |
| 能效 | 0.2 | 能源效率优化 |

---

## 4. 系统整体性能总结

### 4.1 各模块性能汇总（最终真实训练结果, 2026-06-29）

| 模块 | 核心指标 | 数值 | vs 基线 |
|------|----------|------|---------|
| AI 信道估计 | NMSE | **-20.42 dB** | LMMSE: -0.70 dB（提升 19.7 dB） |
| 信号检测 | F1 Score | **98.04%** | — |
| 调制识别 | Overall Acc | **76.91%** | BPSK 98.9%, 256QAM 82.8% |
| 资源调度 | Avg Reward | **2.393** | Jain Fairness 0.8717 |

### 4.2 系统参数配置

| 参数 | 数值 | 符合标准 |
|------|------|----------|
| 载波频率 | 3.5 GHz | C 波段 5G |
| 系统带宽 | 100 MHz | 3GPP 规范 |
| FFT 大小 | 2048 | OFDM 标准 |
| 循环前缀 | 512 | 抗多径 |
| 数据子载波 | 1200 | 有效带宽 |
| 信道模型 | 5G UMa | 城市宏蜂窝 |

---

## 5. 测试环境信息

### 5.1 软件环境

| 组件 | 版本 | 说明 |
|------|------|------|
| Python | 3.13.5 | conda 环境 |
| PyTorch | ≥ 1.12.0 | 深度学习框架 |
| NumPy | ≥ 1.21.0 | 科学计算 |
| SciPy | ≥ 1.7.0 | 信号处理 |
| stable-baselines3 | ≥ 1.5.0 | 强化学习框架 |

### 5.2 训练配置

| 参数 | 数值 | 说明 |
|------|------|------|
| 训练设备 | CUDA (GPU) | NVIDIA GPU |
| 信道估计样本 | 8,000 | 训练集 |
| 信号检测样本 | 5,000 | 训练集 |
| 调制识别样本 | 1,000/类 | 5 类×1000 |
| SNR 范围 | -10 ~ 30 dB | 信噪比范围 |
| 多普勒频率 | 100 Hz | 信道时变特性 |
| 时延扩展 | 300 ns | 多径时延 |
| 随机种子 | 42 | 可复现性 |

### 5.3 运行方式

```bash
# 运行完整仿真
python main.py --mode full

# 单独测试各模块
python -m channel_estimation.trainer --epochs 100
python -m signal_detection.detector --mode test
python -m resource_scheduling.agent --algorithm ppo --timesteps 50000
```

---

**参考文档**: [算法架构文档](./ALGORITHMS.md) | [项目README](../README.md)
